import cp from '@core-ds/primitives';

const Input = {
   parts: ['field'],
   variants: {
      outline: {
         field: {
            bg: cp.color.white,
         },
      },
   },
};

export default Input;
